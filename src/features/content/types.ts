export interface HeroContent {
  title: string;
  subtitle: string;
}

export interface AboutContent {
  title: string;
  description: string;
}

export interface PricingPackage {
  title: string;
  price: string;
  features: string[];
}
